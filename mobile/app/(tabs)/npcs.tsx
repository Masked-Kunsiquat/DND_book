import { Screen, EmptyState } from '../../src/components';

export default function NpcsScreen() {
  return (
    <Screen>
      <EmptyState
        title="No NPCs yet"
        description="NPC list screen is coming next."
        icon="account-group-outline"
      />
    </Screen>
  );
}
